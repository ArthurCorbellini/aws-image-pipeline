aws:
  endpoint-url: ""
  presigned-endpoint-url: ""
  access-key-id: ""
  secret-access-key: ""

# app.s3.bucket-name and app.sqs.queue-url are deliberately NOT set here —
# the queue-url embeds the AWS account ID, which shouldn't go into the public repo.
# Pass them as environment variables at runtime instead: APP_S3_BUCKET_NAME, APP_SQS_QUEUE_URL
